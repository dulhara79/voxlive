# WebSocket URL for the VoxLive backend.
# Development (HTTP):   ws://localhost:8080
# Production (HTTPS):   wss://your-backend-domain.com
#
# NOTE: You MUST use wss:// when your frontend is served over HTTPS.
VITE_WS_URL=ws://localhost:8080
